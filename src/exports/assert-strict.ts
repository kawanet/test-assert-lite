import {strict} from "../index.ts"

// Entry for `test-assert-lite/assert/strict`. As with `node:assert/strict`
// the default export is the assert function itself, methods attached.
export default strict
